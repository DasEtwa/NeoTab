package de.NeoTab.neotab;

import org.bukkit.inventory.InventoryHolder;

/** Marker for inventories owned by NeoTab and safe to close during reload/disable. */
interface NeoTabInventoryHolder extends InventoryHolder {
}
